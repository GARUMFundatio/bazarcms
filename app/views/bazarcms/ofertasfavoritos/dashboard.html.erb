<% cache "bazar_favoritos_ofe_dash_#{current_user.id}", {:expires_in => 30.minutes.to_i} do %>

<h3 class="page-title">Ofertas/Demandas que sigo</h3>

<table class="tabla" cellspacing="0">

<% for fav in @favoritos %>
<tr>

	<td>
		<%= link_to(fav.nombre_empresa, bazarcms_oferta_path(fav.empresa_id, :bazar_id => fav.bazar_id)) %>
	</td>

	<td class="dos-iconos">
		<%= helper_favoofe(fav.bazar_id, fav.empresa_id, fav.nombre_empresa, "cajafavoritos") %>
	</td>

</tr>
<% end %>

</table>
<% if @total > 5 %>
	<%= link_to("Ver más favoritos (#{@total-5}+) &raquo;".html_safe, bazarcms_ofertasfavoritos_path, :class => "volver-home-link") %>
<% end %>

<% end %>