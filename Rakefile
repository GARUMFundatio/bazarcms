begin
  require "jeweler"
  Jeweler::Tasks.new do |gem|
    gem.name = "bazarcms"
    gem.summary = "bazar_CMS engine  for Rails 3"
    gem.files = Dir["{lib}/**/*", "{app}/**/*", "{config}/**/*"]

  end
rescue
  puts "Jeweler o una de sus dependencias no ha sido instalada."

end
